export type TShippingAddress = {
  name: string;
  division: string;
  district: string;
  subDistrict: string; // Array of URLs or paths to product photos
  address: string;
  phone: string;
  createdBy: string;
};
